// Comparisons produce real booleans, which combine with &&, ||, and !.
let ready = 3 >= 2;           // true
let equal = (1 + 1) == 2;     // true
let strict = 5 < 5;           // false
let both = ready && !strict;  // true (true && !false)
let either = ready || strict; // true

both && either
