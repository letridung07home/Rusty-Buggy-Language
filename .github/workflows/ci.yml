name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  stable:
    name: Stable quality checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable
          components: rustfmt, clippy

      # This crate has no external dependencies, so cache the crate's own
      # build artifacts to make re-runs of the same commit cheap.
      - name: Cache Cargo build artifacts
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-workspace-crates: "true"

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check compilation
        run: cargo check --all-targets --all-features

      - name: Run tests
        run: cargo test --all-targets --all-features

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Check documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps --all-features

  coverage:
    name: Line coverage
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install stable Rust with LLVM tools
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable
          components: llvm-tools-preview

      # Caches the installed cargo-llvm-cov binary (~/.cargo/bin). Workspace
      # artifacts are excluded because cargo-llvm-cov rebuilds them with
      # coverage instrumentation anyway.
      - name: Cache Cargo artifacts
        id: rust-cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2

      # Skip the compile when the binary was restored from cache.
      - name: Install cargo-llvm-cov
        if: steps.rust-cache.outputs.cache-hit != 'true'
        run: cargo install cargo-llvm-cov --locked

      # Prints the per-file summary and fails the job when total line
      # coverage drops below 80%, so blind spots are visible in CI. Tune the
      # threshold to the measured baseline. The crate is small, so the test
      # suite runs a second time for the lcov export at negligible cost.
      - name: Generate coverage summary
        run: cargo llvm-cov --all-features --fail-under-lines 80

      - name: Export lcov report
        run: cargo llvm-cov --all-features --lcov --output-path lcov.info

      # Secrets cannot be referenced in `if:` conditionals, so probe the
      # step-level env and gate the upload on its output. v5 also supports
      # tokenless uploads for public repos once the repo is on Codecov.
      - name: Check for Codecov token
        id: codecov-token
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          if [ -n "$CODECOV_TOKEN" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload coverage to Codecov
        if: steps.codecov-token.outputs.configured == 'true'
        uses: codecov/codecov-action@0fb7174895f61a3b6b78fc075e0cd60383518dac # v5.5.5
        with:
          files: lcov.info
          fail_ci_if_error: false

  actionlint:
    name: Lint workflow files
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Catches YAML, expression, and shell errors in workflow files at PR
      # time instead of on a release tag, and annotates PRs via a problem
      # matcher. Uses the actionlint release pinned inside the action.
      - name: Run actionlint
        uses: devops-actions/actionlint@ec02b36684b2f574f1d219ad0a43b082e46bf3e4 # v0.1.13

  msrv:
    name: Rust 1.70 compatibility
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Rust 1.70
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: 1.70.0

      # Cached separately from the stable job via the toolchain-specific key.
      - name: Cache Cargo build artifacts
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-workspace-crates: "true"

      - name: Check compilation
        run: cargo check --all-targets --all-features

      - name: Run tests
        run: cargo test --all-targets --all-features